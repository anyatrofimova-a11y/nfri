# Index history snapshots (IOSCO benchmark requirement)

Publish one JSON snapshot per month: `YYYY-MM.json` with cut-lines, universe hash, and median MoS ranks.

Minimum 12 files required for `OFFERING-IOSCO-BENCHMARK` criterion IG3.

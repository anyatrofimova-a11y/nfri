# Deprecated — parallel wave superseded

**Use `BUILD_SEQUENCE.md` instead.**

This document described all sprints running in parallel. The build now follows Felix's phased sequence:

1. Index credibility  
2. Accumulation graph (RDS acceptance)  
3. Pricing engine  
4. Active bundling + CSaaS embed  

See `BUILD_SEQUENCE.md` for checklists, exit gates, and agent fan-out.
